# PPAP 文件校验平台 - 项目遍历报告

本报告记录了项目当前的代码结构、技术栈设计、已有进度以及未来的关键待办任务，作为项目开发后续的参考基准。

---

## 📂 项目整体目录结构

本平台为一个基于 **PPAP (生产件批准程序) 的智能文件校验平台**。项目包含以下四大核心模块：

```text
ppap/
├── prototype/          # 交互式原型图与产品设计文档
│   ├── index.html     # 可在浏览器中直接打开的高保真交互原型
│   ├── project.md     # 系统设计说明书（色彩、组件规范、校验项定义）
│   └── progress.md    # 开发进度跟踪表
├── backend/           # 基于 Python FastAPI 的异步后端
│   ├── app/           # 后端核心业务逻辑
│   ├── requirements.txt # 后端依赖清单
│   └── Dockerfile     # 后端容器化配置
├── frontend/          # 基于 Vue 3 + TypeScript 的现代前端
│   ├── src/           # 前端源代码（Views/Components/Layouts/Stores）
│   ├── package.json   # 前端依赖配置
│   └── Dockerfile     # 前端容器化配置
└── deploy/            # 容器化部署编排配置
    ├── docker-compose.yml # 包含 PG, Redis, MinIO, Backend, Frontend
    ├── nginx.conf     # 反向代理与动静分离配置
    └── init-db.sql    # 数据库初始化脚本
```

---

## 🖥️ 后端架构深入 (`backend/app`)

后端使用了 **FastAPI + SQLAlchemy 2.0 (异步) + Pydantic v2 + Redis + MinIO** 的现代企业级技术栈：

1. **核心组件 (`core/`)**：
   * `config.py`: 基于 Pydantic Settings 的强类型环境变量与应用配置。
   * `database.py`: 配置了 SQLAlchemy 异步引擎 (`create_async_engine`)。
   * `security.py`: 提供 JWT 编码/解码与密码 Hash 加密。
   * `redis.py`: 封装了基于 `redis-py` 的异步 Redis 客户端连接。
   * `minio_client.py`: 封装了 MinIO 存储服务，负责 PDF 文件生命周期管理。
2. **数据模型与 Schema (`models/` & `schemas/`)**：
   * 包含 `User` (用户表)、`File` (文件表)、`Task` (AI 校验任务表)、`Notification` (系统通知表)、`Note` (审核备注表) 对应的 ORM 模型与 Pydantic 校验模型。
3. **业务逻辑层 (`services/`)**：
   * `file_service.py`: 处理文件上传、生命周期管理与解析。
   * `notification_service.py`: 管理未读消息、标记已读及全局消息分发。
   * `aliyun_service.py`: 预留阿里云 AI 校验引擎的模拟及后期真实集成逻辑。
4. **API 控制层 (`api/`)**：
   * 细分为 `auth`、`files`、`notifications`、`notes` 等路由模块，职责清晰。

---

## 📌 当前项目开发进度与待开发项

目前**核心框架、核心核查算法、数据库持久化系统、前端高保真交互与关键运行时缓存 Bug 已全面修复并运行畅通**，后续的关键开发任务（按照优先级排序）包括：

### 🚀 已完成的重要里程碑
1. **🎨 登录页面视觉与交互高级重构**：
   * 移除了动态流动极光背景，改用扁平沉稳的紫蓝渐变背景。
   * 右侧操作登录区重新打造为精致的高级悬浮质感卡片，配合圆角圆滑设计 (`border-radius: 28px`)。
2. **🔌 数据库端口冲突与类型崩溃修复**：
   * 将容器 PostgreSQL 的本地映射端口调整为 `5435`，避开冲突。
   * 修改 SQLAlchemy 模型主键及外键 ID 类型为 `UUID(as_uuid=False)`。
3. **💻 前端核心展示组件补全与 Runtime 闪白修复**：
   * 全面实现并挂载了 `TaskList.vue` 与 `NotificationList.vue` 核心组件。
   * 修复了全局 runtime 引用崩溃与路由冲突。
4. **⚙️ 接口请求传参参数类型订正**：
   * 修复了文件查询接口的参数类型错误，为 demo 注册用户添加角色检查。
5. **🛡️ 本地化 PDF 核心合规校验引擎与 Celery Worker 稳健集成**：
   * 编写四大核心高精本地化核查模块：文本型 PDF 结构判定、国密与标准电子签名防篡改验证、二维码溯源提取、防增删单页替换交叉核查。
   * 根治连接池死锁顽疾，实现多阶段状态机流水线的实体数据对接。
6. **⚡ 任务看板与历史条件查询高级重构 (Aesthetics & UX)**：
   * 上传区与看板分配重构为 **40/60 黄金分栏**，**2列 CSS 网格流**。
   * 添加“全部”选项卡，升级查询表单为可折叠条件筛选中心。
7. **📊 顶级 23 列多维合规诊断审计报表 (Excel Exporter)**：
   * 支持带有 UTF-8 BOM 的 23 列精细 Excel 审计报表导出。
8. **⚙️ 数据库持久化 SMTP 设置与动态系统邮件通知**：
   * 创建并迁移 `settings` 表保存全局设置，脱离环境变量限制。
   * 支持前端脱敏修改密码，解耦邮件服务并通过 Celery 流水线联通。
9. **🛡️ 增强版 PDF 数字证书多维提取与折叠式 UI 审计看板**：
   * 全异步提取证书签署主体、CA 机构与时效。
   * 前端实现默认隐藏、点击平滑拉展的折叠式证书详情看板交互。
10. **🗑️ PDF文件自动清理功能**：
    * 基于 Celery Beat 实现定时任务调度，自动清理 MinIO 中超过保留期限的 PDF 文件。
    * 前端新增文件保留设置页面，支持管理员自定义保留天数、清理触发时间。
11. **🧩 可视化规则引擎节点图修复**：
    * 修复了规则配置页面中“可视化节点图”（VueFlow）无法加载的 Bug，解决了初始化时序与组件导入问题。
12. **📦 数据库迁移机制 (Alembic)**：
    * 成功配置 Alembic 并生成了初始的迁移脚本，为后续业务演进提供了物理数据库的结构演进与版本管理支持。
13. **👁️ PDF.js 在线预览与问题高亮**：
    * 前端在文件详情页已深度集成 `pdfjs-dist`，实现了 PDF 的在线渲染。
    * 基于后端提取的坐标（如签名、二维码等位置），开发了 `drawHighlightBox` 动态在 PDF 页面上高亮绘制异常与校验信息区域。
14. **🧠 动态验证规则引擎与可视化 DAG 工作流**：
    * 实现了灵活的规则引擎及 Operator 设计模式，支持基于 DAG（有向无环图）的逻辑验证节点，新增了文本与视觉大模型（LLM）执行算子。
    * 前端新增规则配置页面，支持拖拽和节点图形式动态管理核查规则。
15. **🧑‍⚖️ Human-in-the-loop 人工审核工作流**：
    * 在自动校验任务流中加入了“需要人工审核 (Needs Review)”状态，实现机器预判+人工复核的闭环工作流。
16. **🖨️ PDF 打印报告与分页导航**：
    * 实现了诊断检查的分页导航功能，并在文件详情页支持 PDF 结果报告打印功能。

### 优先级 P1 (重要功能)
1. **实时进度推送** - 引入 WebSocket 或 SSE 机制，实现后台校验进度向前端的实时推送。

### 优先级 P2 (增强体验)
2. **审计日志追踪** - 记录用户的敏感操作及系统校验轨迹。
